# Alembic configuration. The database URL is injected from the app settings
# (DATABASE_URL) in migrations/env.py, so it is intentionally not set here.
[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
